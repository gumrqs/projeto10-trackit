    function criarHabito(resposta){
         if(resposta==="clicado"){
            setAdicionarHabitos(
            <CriandoHabito>
                <Dados /* onSubmit={confirmarLogin} */>
                    
                        <Forms>
                                <Input type="text"  /* onChange={(e) => setEmailUsuario(e.target.value)} 
                                   value={emailUsuario}  */
                                    required
                                    placeholder='nome do hábito'
                        
                                />
                        </Forms>
                        <DiasSemana>
                            <Help onClick={()=> selecionarData(1)}>
                                <DiaNselecionado click={selecionarDia} >
                                            D
                                </DiaNselecionado>
                            </Help>
                        </DiasSemana>
                         <ComponenteBotao>
                             <Cancelar onClick={()=>criarHabito("cancelado")}>
                             cancelar
                             </Cancelar>
                         <Button type='submit'> <p>Salvar</p></Button>
                         </ComponenteBotao>
                </Dados>
            </CriandoHabito>);
        } 
        if(resposta==="cancelado"){
            setAdicionarHabitos('')
        }
    }